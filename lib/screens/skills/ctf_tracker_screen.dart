import 'package:ciaraos/widgets/common/stub_screen_scaffold.dart';
import 'package:flutter/material.dart';

class CtfTrackerScreen extends StatelessWidget {
  const CtfTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StubScreenScaffold(
      message: 'CTF Tracker — coming soon\nTrack HackerOne progress here.',
    );
  }
}
